name: demo_app
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  # UI and Asset Packages
  rive: ^0.13.20
  cupertino_icons: ^1.0.2

  # Firebase Packages (Corrected and Updated)
  firebase_core: ^2.32.0      # Essential for all Firebase services
  firebase_auth: ^4.20.0      # For user authentication
  cloud_firestore: ^4.17.5  # For the Firestore database


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # Assets (Corrected)
  # Including the folder is enough to include all files within it.
  assets:
    - assets/